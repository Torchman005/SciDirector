# 导演智能体 —— 用户消息模板

请把下面的科普脚本拆解为分镜表。

## 原始脚本

```
{{raw_script}}
```

## 参数

- 目标总时长：{{target_duration_sec}} 秒
- 语言 / 地区：{{locale}}

## 风格约束

{{style_guide}}

## 现在开始

先在心里完成这三步（**不要写出来**）：
1. 数一数脚本里有多少个**独立的视觉单元**（一个公式推导、一组数据、一段代码、一次转折）；
2. 用 `目标总时长 / 视觉单元数` 估算平均镜头时长，据此决定是否需要合并或拆分；
3. 为每个视觉单元选定唯一标签，并写出一个**可执行**的视觉意图。

然后**只输出 JSON**（格式见系统提示）。不要输出任何解释、前言或后记。
