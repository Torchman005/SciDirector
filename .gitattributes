# =============================================================================
# 统一换行符策略
# -----------------------------------------------------------------------------
# 为什么必须有这个文件：
#   Windows 默认 core.autocrlf=true，会把工作区的换行转成 CRLF。
#   对 Makefile 来说这是**致命的** —— CRLF 会让 make 报
#   "missing separator"，而这类问题只在 Linux/CI 上才暴露，
#   本地一切正常，排查成本极高。
#
# 策略：仓库内一律存 LF，检出时也保持 LF（eol=lf），
#       让 Windows / Linux / macOS / 容器内的行为完全一致。
# =============================================================================

* text=auto eol=lf

# ---- Windows 批处理：必须是 CRLF，不能跟随全局的 eol=lf ----
#
# cmd.exe 对 **LF-only 的 .bat/.cmd 存在已知的处理缺陷**：它会吃掉某些行的
# 行首字符，报出完全看不懂的错（本项目实测到 `setlocal` 变成 `etlocal`、
# `echo` 变成 `cho`、`set "A=B"` 里的键名丢首字母）。
#
# 这类故障的排查成本极高 —— 报错指向的字符和真正的原因毫无关系，
# 而且同一份脚本在记事本里"看起来完全正常"。
#
# 因此这里显式覆盖：仓库内按 CRLF 存储，检出时也是 CRLF。
*.cmd text eol=crlf
*.bat text eol=crlf

# ---- 二进制：不参与任何换行转换 ----
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.webp binary
*.ico  binary
*.mp4  binary
*.mkv  binary
*.wav  binary
*.mp3  binary
*.woff binary
*.woff2 binary
*.ttf binary
*.exe  binary
*.dll  binary
*.so   binary
*.zip  binary

# ---- 生成代码：GitHub 上折叠 diff（机器产物，人工不逐行看）----
backend/internal/pb/**   linguist-generated=true
ai/scidirector_ai/pb/**  linguist-generated=true

# ---- 锁文件：同样折叠，避免污染 PR 视图 ----
backend/go.sum           linguist-generated=true
